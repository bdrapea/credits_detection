#pragma once

#include <vector>

namespace crde
{
    template<typename T>
    std::vector<T> longest_common_subseq(const std::vector<T>& subseq1,
                                         const std::vector<T>& subseq2,
                                         const T threshold)
    {
        std::vector<T> common_subseq;

        return common_subseq;
    }
}
